# 🚀 QUICK START - AI Agent с Vision & Telegram

## 1. Основная миссия

Агент открывает приложения (Роблокс, Chrome), **видит** экран, кликает, вводит данные, **работает самостоятельно**.

## 2. Компоненты

```
backend/
├── vision_controller.py    # 👁 Анализ экрана + определение объектов по цвету
├── autonomous_agent.py     # 🤖 Автономный агент (предлагает идеи, работает)
├── telegram_bot.py         # 📱 Управление через Telegram
└── agent.py               # REST API эндпойнты
```

## 3. Установка

```bash
# Клонирую
 git clone https://github.com/Fisterovna2/perplexity-local-agent
 cd perplexity-local-agent

# Пакеты
 pip install -r backend/requirements.txt
```

## 4. Как запустить

### Вариант 1: Локально (Python)
```bash
python backend/agent.py
# Агент стартует на http://localhost:5000
```

### Вариант 2: Docker
```bash
docker-compose up --build
```

## 5. Команды Telegram

Отправь в Telegram чат:

| Команда | Мото |
|-----------|-------|
| `/start` | Помощь |
| `/propose` | Предложить новый проект |
| `/approve` | Одобрить и выполнить |
| `/reject` | Отклонить |
| `/auto_on` | Автомод: агент определяет идеи сам особо автоматических идей |
| `/auto_off` | Отключить автомод |
| `/stats` | Статистика |

## 6. Примеры

### Эо1: Открыть Roblox и пройти обби в автомоде

1. `/propose` - Получить оферту
2. `/approve` - Одобрить работает
3. Агент откроет Roblox, **анализирует** экран, кликает на зеленые кнопки

## 7. Настройка Bot

Сохрани токен телеграм бота в `.env`:

```
TELEGRAM_BOT_TOKEN=your_token_here
ALLOWED_USER_IDS=123456789
```

## 8. АРХИТЕКТУРА

```
ТЕЛЕГРАМ (голос) 
     ↓
   РОУТЕР API (/api/v1/telegram/command)
     ↓
ТЕЛЕГРАМ БОТ КОНТРОЛЛЕР
     ↓
АВТОНОМНЫЙ АГЕНТ (GenerateIdeas)
     ↓
ВИЗИОН + INPUT КОНТРОЛЛЕР (Открывают экран)
     ↓
ПК (Мышь, Видим объекты)
```

## 🚀 Готово! Всё работает!


## 🛡️ 7. Модули безопасности

### Internet Safety (VirusTotal)

**Проверка файлов перед выполнением:**

```python
from backend.internet_safety import VirusTotalScanner

scanner = VirusTotalScanner("YOUR_API_KEY")
result = scanner.scan_file("/path/to/file.exe")

if result['status'] == 'success':
    print(f"Файл безопасен! SHA256: {result['sha256']}")
else:
    print(f"ОПАСНЫЙ ФАЙЛ! {result['message']}")
```

**Настройки в config.yaml:**

```yaml
safety:
  virustotal:
    enabled: true
    api_key: "YOUR_VIRUSTOTAL_API_KEY"
    scan_timeout: 120
    auto_delete_dangerous: true
```

**Получить API ключ:** https://www.virustotal.com/

### Confirmation System

**Подтверждение перед действиями на ПК:**

Агент запрашивает разрешение ПЕРЕД каждым действием:
- ✅ Открытие/изменение файлов
- ✅ Запуск программ
- ✅ Скачивание файлов
- ✅ Действия в играх (Roblox и др.)
- ✅ Управление мышью/клавиатурой

**Диалог подтверждения включает:**
- 📝 Описание действия
- ℹ️ INFO кнопка с детальной информацией
- ✅ APPROVE / ❌ DENY кнопки
- ⏱️ Таймаут (60 сек)

**Пример использования:**

```python
from backend.confirmation_system import ConfirmationSystem, confirm_file_operation

confirmation = ConfirmationSystem()

# Перед операцией с файлом
if confirm_file_operation(confirmation, "write", "document.txt"):
    # Пользователь одобрил
    with open("document.txt", "w") as f:
        f.write("данные")
else:
    print("Действие отменено пользователем")
```

### Self-Protection

**Защита от самомодификации:**

Агент НЕ МОЖЕТ редактировать собственный код:
- 🛡️ Блокирует доступ к `backend/` директории
- 🛡️ Защищает критические файлы (agent.py, config.yaml)
- 🛡️ Проверяет целостность через SHA256 хеши
- 🛡️ Только чтение разрешено

**Защищенные файлы:**
- `agent.py` - основной агент
- `self_protection.py` - модуль защиты
- `confirmation_system.py` - система подтверждений
- `internet_safety.py` - проверка безопасности
- `config.yaml` - конфигурация

**Пример:**

```python
from backend.self_protection import SelfProtection

protection = SelfProtection()

# Попытка изменить собственный код
allowed, reason = protection.validate_operation("write", "backend/agent.py")
if not allowed:
    print(reason)  # ❌ BLOCKED: Cannot write critical file agent.py
```

## 🎮 8. Работа с играми (Roblox)

Агент может:
1. **Открывать игры** - запускает Roblox и нужную игру
2. **Видеть экран** - анализирует через vision_controller
3. **Управлять** - мышь + клавиатура
4. **Фармить** - автоматически играет (с вашим разрешением)

⚠️ **ВАЖНО:** Перед каждым действием в игре агент спросит разрешение!

## 🤖 9. Автономный режим

Агент может работать самостоятельно:
- 💡 Придумывает проекты
- 📋 Предлагает идеи пользователю
- ✅ Начинает работу после одобрения
- 🔄 Работает в фоне

**Включить в config.yaml:**

```yaml
autonomous:
  enabled: true
  auto_propose_projects: true
  require_approval: true
```

## 📱 10. Telegram управление

**Управление агентом через Telegram бот:**

1. Создайте бота через @BotFather
2. Добавьте токен в config.yaml:

```yaml
telegram:
  enabled: true
  bot_token: "YOUR_BOT_TOKEN"
  allowed_users: [123456789]  # Ваш Telegram ID
```

3. Запустите агента
4. Отправьте команды боту:
   - `/start` - активировать
   - `/status` - статус агента
   - `/task <описание>` - дать задание

## ⚠️ 11. Важные замечания

### Безопасность:

✅ **ВСЕГДА включено:**
- Подтверждение перед действиями
- VirusTotal проверка файлов
- Защита от самоизменения
- Логирование всех действий

❌ **Агент НЕ МОЖЕТ:**
- Редактировать свой код
- Выполнять непроверенные файлы
- Делать что-либо без вашего разрешения (когда require_confirmation: true)

### Производительность:

- Vision: ~1 скриншот/сек
- Подтверждения: таймаут 60 сек
- VirusTotal: ~2 мин на файл

## 📞 12. Поддержка

Если что-то не работает:

1. Проверьте логи: `backend/logs/`
2. Убедитесь что все зависимости установлены
3. Проверьте config.yaml настройки
4. API ключи (VirusTotal, Telegram) правильные

---

**🚀 Теперь у вас полнофункциональный AI-агент с максимальной безопасностью!**
