# 🚀 Perplexity Local Agent - Setup Guide

## Что это?
**Perplexity Local Agent** - это локальный агент для интеграции Perplexity с вашим ПК. Он позволяет нейронке:
- Генерировать 3D модели через Blender
- Создавать полные игровые проекты (Unity/Godot)
- Выполнять Python скрипты
- Открывать программы и файлы
- Управлять вашим компьютером с полным контролем безопасности

## Требования
- Python 3.8+
- Windows/MacOS/Linux
- Flask
- Blender (опционально, для 3D)
- Tampermonkey (браузер расширение)

## Установка

### 1. Клонируй репозиторий
```bash
git clone https://github.com/Fisterovna2/perplexity-local-agent.git
cd perplexity-local-agent
```

### 2. Установи зависимости
```bash
pip install -r backend/requirements.txt
```

### 3. Запусти Backend
```bash
python backend/agent.py
```
Агент запустится на `http://localhost:5000`

### 4. Установи Tampermonkey скрипт
1. Установи [Tampermonkey](https://www.tampermonkey.net/)
2. Скопируй содержимое из `tampermonkey/perplexity_bridge.js`
3. Создай новый скрипт в Tampermonkey
4. Вставь код
5. Перезагрузи страницу Perplexity

### 5. Используй в Perplexity
В чате Perplexity используй специальные команды:

```
[ACTION:BLENDER]
import bpy
# твой код Blender...
[/ACTION]

[ACTION:PYTHON]
# твой Python код...
[/ACTION]

[ACTION:OPEN]
program_name or file_path
[/ACTION]
```

## Безопасность
✅ **Перед каждым действием** агент запрашивает подтверждение
✅ **INFO кнопка** показывает подробное описание
✅ **Whitelist** - только разрешенные команды
✅ **Логирование** - все действия записываются
✅ **Локально** - ничего не отправляется на сервера

## Архитектура
- **Backend**: Flask REST API на порту 5000
- **Frontend**: Веб-интерфейс с управлением
- **Tampermonkey**: Интеграция с Perplexity
- **Config**: Белый список команд в `config.yaml`

## Конфигурация
Отредактируй `backend/config.yaml` для:
- Добавления новых команд
- Изменения timeout
- Настройки безопасности

## Примеры использования

### Создание 3D модели
```
Напиши Blender скрипт для создания куба и экспортируй его в .obj
```

### Запуск Python скрипта
```
Напиши Python скрипт, который обрабатывает данные из файла
```

### Открытие приложения
```
Открой программу Discord
```

## Лицензия
MIT License. Используй свободно, но не распространяй без указания авторства.

## ⚠️ ВАЖНО
- Это инструмент для **опытных пользователей**
- Убедись что понимаешь код перед выполнением
- **Никогда** не запускай неизвестный код
- Агент может удалить файлы - используй с осторожностью!

## Поддержка
Если есть вопросы - open issue на GitHub 👉
